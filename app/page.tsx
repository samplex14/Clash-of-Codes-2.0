import HomeRegisterClient from "@/components/HomeRegisterClient";

const HomePage = (): JSX.Element => {
  return (
    <div className="flex flex-col items-center justify-center min-h-[80vh]">
      <HomeRegisterClient />
    </div>
  );
};

export default HomePage;
